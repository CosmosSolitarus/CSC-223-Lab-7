package utilities;

import java.util.Set;
import java.util.SortedSet;

import geometry_objects.points.Point;

public class PointUtilities {
    public static SortedSet<Point> sortPoints(Set<Point> points) {
        int n = points.size();
        Point temp;

        for (int i = 0; i < n; i++) {
            for (int j = 1; j < n-1; j++) {
                if (points.)
            }
        }
    }
}
